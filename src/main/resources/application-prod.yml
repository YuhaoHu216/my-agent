# ============================================
# 生产部署环境（Linux / Docker）
# 激活方式: spring.profiles.active=prod
# ============================================

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/my_agent?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: Zf9@Qv7!Lm2Xc8$HpT8+rK!2pWq9@Vx6$Mn
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: redis
      port: 6379
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  ai:
    mcp:
      client:
        stdio:
          servers-configuration: classpath:mcp-servers-linux.json  # Linux 用 npx

milvus:
  host: milvus
  port: 19530

# 向量数据库的选择 simple | milvus 等换内存更大的服务器在用milvus
vector:
  store:
    type: simple
