# ============================================
# 本地开发环境（Windows）
# 激活方式: spring.profiles.active=dev（默认）
# ============================================

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/my_agent?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: Zf9@Qv7!Lm2Xc8$HpT8+rK!2pWq9@Vx6$Mn
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  ai:
    mcp:
      client:
        stdio:
          servers-configuration: classpath:mcp-servers.json   # Windows 用 npx.cmd

milvus:
  host: localhost
  port: 19530

# 向量数据库：开发环境使用内存向量库
vector:
  store:
    type: simple
