<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="space.huyuhao.myagent.mapper.UserSkillMapper">

    <select id="selectByUserId" resultType="space.huyuhao.myagent.entity.UserSkill">
        SELECT * FROM user_skill WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="selectEnabledByUserId" resultType="space.huyuhao.myagent.entity.UserSkill">
        SELECT * FROM user_skill WHERE user_id = #{userId} AND enabled = 1 ORDER BY create_time DESC
    </select>

    <select id="selectByUserIdAndId" resultType="space.huyuhao.myagent.entity.UserSkill">
        SELECT * FROM user_skill WHERE id = #{id} AND user_id = #{userId}
    </select>

    <select id="selectByUserIdAndName" resultType="space.huyuhao.myagent.entity.UserSkill">
        SELECT * FROM user_skill WHERE user_id = #{userId} AND skill_name = #{skillName}
    </select>

</mapper>