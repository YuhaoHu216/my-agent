<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="space.huyuhao.myagent.mapper.UserDocumentMapper">

    <select id="selectByUserId" resultType="space.huyuhao.myagent.entity.UserDocument">
        SELECT * FROM user_document WHERE user_id = #{userId} AND status = 1 ORDER BY create_time DESC
    </select>

    <select id="selectByUserIdAndId" resultType="space.huyuhao.myagent.entity.UserDocument">
        SELECT * FROM user_document WHERE id = #{id} AND user_id = #{userId} AND status = 1
    </select>

</mapper>
